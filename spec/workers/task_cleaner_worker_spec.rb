require 'rails_helper'
RSpec.describe TaskCleanerWorker, type: :worker do
  it "perform delete" do
    pending ('not realized yet')
    fail()
    # task = FactoryBot.create(:task)
    # TaskCleanerWorker.new.perform(task.id)
    # expect(task).to be_nil
  end
end
